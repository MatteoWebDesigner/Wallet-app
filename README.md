Wallet Web App
==============

location:
the application is inside "walletwebapp" folder

prepare enviroment
	npm install & bower install

start server
	grunt serve

build PRD 
	grunt build

Dependencies:
	Yeoman,
	Grunt,
	Bower,
	Bootstrap,
	FontAwesome,
	Angular,
	grunt-ng-template